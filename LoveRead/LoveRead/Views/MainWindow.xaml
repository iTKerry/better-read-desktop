<Window x:Class="LoveRead.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LoveRead" Height="500" Width="800"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" MinWidth="250" MaxWidth="450" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="10">
            <TextBox IsEnabled="True" Text="{Binding BookUrl, UpdateSourceTrigger=PropertyChanged}" />
            <Button Command="{Binding ReadBookCommand}" IsEnabled="{Binding IsReadButtonEnabled}" Content="Read"/>
            <Button Command="{Binding GenerateDocCommand}" Content="Create docx"/>
        </StackPanel>
        <GridSplitter Grid.Column="1" Width="5" />
        <ScrollViewer x:Name="LogList" Grid.Column="2" VerticalScrollBarVisibility="Visible" Margin="10">
            <ItemsControl ItemsSource="{Binding LogList}" />
        </ScrollViewer>
    </Grid>
</Window>
